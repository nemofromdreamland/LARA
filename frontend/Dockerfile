FROM node:20-alpine AS builder

WORKDIR /app

# Placeholder — React app scaffold added in Step 8
# This stage will run: npm install && npm run build

FROM nginx:alpine

# Placeholder config — replaced in Step 8 with actual build output
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 5173
